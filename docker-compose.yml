services:
  velomate:
    build: .
    ports:
      - "2012:2012"
    volumes:
      - ./data:/app/backend/data   # SQLite database
      - ./logs:/app/logs           # Winston log files
    environment:
      - LOG_DIR=/app/logs
    restart: unless-stopped
