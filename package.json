{
  "name": "innerjoin-root",
  "version": "1.0.0",
  "description": "InnerJoin Garmin Training Root",
  "scripts": {
    "install-all": "cd backend && npm install",
    "dev": "cd backend && npm run dev",
    "start": "cd backend && npm start",
    "build": "cd backend && npm run build"
  }
}
